# Task-Manager
The Task Manager is a simple Python app to manage daily tasks. It includes a CLI for text based commands and a Tkinter GUI for an easy to use interface. Tasks are saved in a file (JSON/text) so they’re not lost when the app is closed.
